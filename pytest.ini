[pytest]
testpaths = tests
# Flat modules imported by name (import spenvis_io, import conduction as cd),
# so put both package folders on the path instead of restructuring into packages.
pythonpath = SPENVIS/SPENPY Thermal_Engineering/THERMPY
